import {Container, Data} from './Elements';

function TextBox({height}) {
  return (
    <Container height={height}>
      <Data>Hello Brother, what is good</Data>
    </Container>
  );
}

export default TextBox;
